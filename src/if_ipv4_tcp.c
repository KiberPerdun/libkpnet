//
// Created by KiberPerdun on 14.11.24.
//

#include "if_packet.h"

/*
bool
if_ipv4_tcp (u0 *packet, u64 size, connection_args_t *args)
{
  if (if_ipv4 (packet, size, args))
    if (if_tcp (packet + sizeof (mac_t) + sizeof (ipv4_t), size - sizeof (mac_t) - sizeof (ipv4_t), args))
      return true;

  return false;
}
*/
